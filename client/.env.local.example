# URL of the Express API
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# --- Optional: only needed if you switch to Clerk for authentication ---
# When both keys below are unset, the app runs with the built-in local
# email/password auth (JWT issued by the Express API).
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
